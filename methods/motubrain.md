# MotuBrain: An Advanced World Action Model

Source text: `MotuBrain.txt`

This file is generated by `scripts/extract-method-sections.mjs`. It extracts method-like sections plus keyword windows for architecture, training, tokenizer, loss, data, and runtime details.

## Extract 1: keyword window

Lines 1-36

```text
    1: MotuBrain: An Advanced World Action Model
    2: for Robot Control
    3: MotuBrain Team
    4: https://www.shengshu.com/en/motubrain
    5: 1
    6: Introduction
    7: Recent progress in embodied foundation models has been driven by Vision-Language-Action (VLA) policies [6,
    8: 8, 10, 20, 22, 26, 33, 38], which map visual observations and language instructions in pretrained Vision-
    9: Language Models (VLMs) [1, 4] directly to robot actions. Inheriting rich semantic priors, VLAs generalize well
   10: across diverse objects and language instructions, achieving strong performance on a wide range of robotic
   11: tasks. However, since they are primarily pretrained on static image–text data, they often neglect the perception
   12: and prediction of fine-grained world dynamics that are essential for precise robotic control, leading to a
   13: superficial imitation of behaviors rather than temporal understanding of the physics of the world.
   14: With the rise of video generation models [3, 23, 30, 34, 39], a growing body of research has begun exploring
   15: how to adapt these models for world modeling [9, 15–18, 28]. A world model aims to predict how the
   16: environment evolves in response to actions—a capability that aligns directly with what a video generation
   17: model does when conditioned on past observations and actions, namely forecasting future visual states.
   18: Conceptually, video generation models are naturally suited to this task, as video generation models are
   19: pretrained on vast and diverse web video data, which equips them with rich spatiotemporal priors regarding
   20: object permanence, physical dynamics, and human-object interactions. This enables them to generalize more
   21: effectively and reason about novel scenarios where in-domain data are scarce. As a result, leveraging video
   22: generation models as a foundation for world modeling has emerged as a promising paradigm for scalable
   23: robot policy learning.
   24: Following this insight, early attempts largely followed a two-stage paradigm: Video Generation Model (VGM)
   25: plus Inverse Dynamics Model (IDM) [13, 14, 19, 29, 40]. In this framework, a video diffusion model
   26: pretrained on large-scale web data first predicts future visual trajectories from current observations and
   27: language instructions. Subsequently, an inverse dynamics model infers actions from the generated future
   28: frames. While this paradigm successfully leverages rich spatiotemporal priors from video data to achieve
   29: broad generalization, it suffers from a critical drawback in that errors in video prediction accumulate over
   30: time, which leads to compromised action accuracy and downstream policy performance.
   31: To mitigate this issue, a subsequent line of research has explored World Action Models (WAMs) that unify visual
   32: dynamics and action prediction within a single, jointly optimized objective [5, 21, 24, 25, 27, 36, 37, 41].
   33: Unlike the VGM+IDM pipeline, which decouples forecasting from action inference, WAMs simultaneously
   34: predict future visual states and actions in an aligned manner. This integration offers two key advantages: (1)
   35: it avoids the cascading errors inherent in sequential video prediction, and (2) it overcomes the fragmented
   36: functionality of conventional embodied systems, where semantic understanding, dynamics modeling, and
```

## Extract 2: keyword window

Lines 55-94

```text
   55: of-Transformers architecture to integrate video generation, action modeling, and language conditioning
   56: within one system. This unified formulation again supports inference over five distributions with the same
   57: model: vision-language-action policy modeling, world modeling, video generation, inverse dynamics, and joint
   58: video-action prediction. More importantly, it preserves the key advantage of unified world-action modeling:
   59: the model can learn from a much broader family of multimodal data, including video-only data without action
   60: labels, interaction data without explicit task language, and heterogeneous robot trajectories collected from
   61: different embodiments.
   62: MotuBrain further extends this paradigm in several practically important directions. It introduces a unified
   63: multiview representation that supports an arbitrary number of camera views under different camera layouts,
   64: rather than depending on a fixed visual input format. It uses an independent text stream to more tightly couple
   65: high-level semantics with low-level control, making instruction following an explicit part of action generation
   66: and improving semantic understanding. It adopts a unified action representation across embodiments, enabling
   67: the model to capture transferable control regularities rather than overfitting to the action format of a single
   68: robot. Beyond architecture and pre-training, MotuBrain also involves a post-training and deployment recipe
   69: tailored to long-horizon real-world control: autoregressive diffusion rollout enables temporally extended
   70: execution, V2A-style asymmetric dependency enables action-only inference without explicitly generating future
   71: video, and real-time chunked closed-loop execution reduces boundary discontinuities during asynchronous
   72: control. Finally, we develop a systems-oriented inference stack including denoising-step reduction, compilation
   73: with CUDA-graph-friendly execution, FP8 quantization, and DiT caching, which together deliver more than
   74: 50× end-to-end speedup over the naive baseline and make large world action models practical for real-time
   75: robotic deployment.
   76: These design choices lead to strong empirical performance across both action-centric and world-modeling
   77: evaluations. On RoboTwin 2.0, MotuBrain achieves average success rates of 95.8% and 96.1% under the clean
   78: and randomized settings, respectively, with the randomized score exceeding 95%. On WorldArena, MotuBrain
   79: attains the strongest reported EWMScore in our comparison, indicating that the same model not only executes
   80: actions effectively but also predicts future world dynamics accurately. Beyond standardized benchmarks,
   81: MotuBrain can be adapted to new humanoid embodiments with only 50–100 same-embodiment trajectories,
   82: while retaining the ability to solve long-horizon and dexterous manipulation tasks without relying on an
   83: additional VLM planner, a dual-system decomposition, external memory, or retry-specific data. Together, these
   84: results suggest that unified world action models can simultaneously scale in generality, controllability, and
   85: real-world deployability.
   86: 2
   87: Method
   88: In this section, we present the overall methodology of MotuBrain from four aspects: model architecture,
   89: pre-training, post-training, and inference. We first introduce the core architectural design and the main
   90: acceleration techniques integrated into MotuBrain in Section 2.1. We then describe the pretrained foundation
   91: model used in our system in Section 2.2. After that, we present the post-training pipeline, including sparse
   92: adaptation and step distillation, in Section 2.3. Finally, we describe the inference-time acceleration strategies
   93: deployed in MotuBrain in Section 2.4.
   94: 2
```

## Extract 3: Method; Model Architecture

Lines 87-156

```text
   87: Method
   88: In this section, we present the overall methodology of MotuBrain from four aspects: model architecture,
   89: pre-training, post-training, and inference. We first introduce the core architectural design and the main
   90: acceleration techniques integrated into MotuBrain in Section 2.1. We then describe the pretrained foundation
   91: model used in our system in Section 2.2. After that, we present the post-training pipeline, including sparse
   92: adaptation and step distillation, in Section 2.3. Finally, we describe the inference-time acceleration strategies
   93: deployed in MotuBrain in Section 2.4.
   94: 2
   95: 
   96:   
   97:   
   98:   
   99: Video  Model
  100: Action   Model
  101: t
  102: t
  103: Language  Model
  104: Multi-Modal Self-Attention
  105: Text Encoder
  106: Video Encoder
  107: Action Encoder
  108: Instruction: Insert the 
  109: flowers into the vase, 
  110: spray water with the 
  111: watering can.
  112: QKV
  113: QKV
  114: FFN
  115: QKV
  116: FFN
  117: FFN
  118: video
  119: action
  120:  Language Block
  121:  Video Block
  122:  Action Block
  123:  Language Block
  124:  Video Block
  125:  Action Block
  126: L×
  127: N×
  128: M×
  129: Figure 1 Overview of MotuBrain’s architecture. MotuBrain builds on a unified video-action backbone and adopts a
  130: three-stream Mixture-of-Transformers architecture with text, video, and action streams. It further uses an H-bridge
  131: attention design to balance cross-modal interaction and efficiency, while supporting flexible multiview inputs through
  132: view-dependent 3D RoPE offsets.
  133: 2.1
  134: Model Architecture
  135: MotuBrain first adopts UniDiffuser [2] to jointly model and schedule the two continuous modalities, i.e., video
  136: and action, so that all interaction patterns between them are captured in a unified generative framework. As a
  137: result, a single training procedure supports inference over five distributions: vision-language-action policy
  138: modeling, world modeling, video generation, inverse dynamics, and joint video-action prediction, which are
  139: formulated in Table 1.
  140: On top of this unified video-action backbone, MotuBrain introduces a three-stream Mixture-of-Transformers
  141: (MoT) architecture with a text stream, a video stream, and an action stream. The text stream serves as a
  142: conditioning branch: its hidden states participate in Transformer attention, but no output head is applied
  143: to text tokens. Maintaining a dedicated text stream also improves semantic understanding and instruction-
  144: following capability. The video and action streams are both trained with flow matching, predicting the velocity
  145: fields of video latents and action tokens, respectively.
  146: The inputs consist of text tokens, condition-image latents encoded by a Vidu VAE, noisy future video latents,
  147: and noisy action tokens. The condition image is represented as the first video latent frame and is teacher-forced
  148: in the video stream, while the remaining video latents and action tokens are denoised by the video and action
  149: streams. Cross-modal interaction is implemented through joint attention over video, action, and text tokens.
  150: Instead of using full video-action joint attention in all layers, we adopt an H-bridge design following
  151: HBridge [35]. Concretely, full V-A joint attention is applied only in the middle 50% of Transformer lay-
  152: ers, while the bottom and top 25% layers use decoupled attention. In the decoupled layers, video tokens and
  153: action tokens are processed independently and do not perform joint-attention. This design reduces the cost
  154: 3
  155: 
  156: Table 1 Different prediction modes (non-autoregressive mode).
```

## Extract 4: keyword window

Lines 154-193

```text
  154: 3
  155: 
  156: Table 1 Different prediction modes (non-autoregressive mode).
  157: Model
  158: Prediction Objective
  159: VLA
  160: p(at+1:t+k | ot, ℓ)
  161: WM
  162: p(ot+1:t+k | ot, at+1:t+k)
  163: IDM
  164: p(at+1:t+k | ot:t+k)
  165: VGM
  166: p(ot+1:t+k | ot, ℓ)
  167: Joint Video-Action Prediction
  168: p(ot+1:t+k, at+1:t+k | ot, ℓ)
  169: of dense cross-modal attention, improves efficiency, preserves modality-specific representations in shallow
  170: and deep layers, and avoids injecting excessive modality-irrelevant information into every layer, while still
  171: allowing semantic alignment and policy grounding in intermediate layers.
  172: For multiview inputs, each view is encoded independently by the Vidu VAE and concatenated at the token
  173: level. Leveraging 3D RoPE, we introduce view-dependent offsets only along the spatial dimensions while
  174: keeping the temporal dimension unchanged. This effectively maps different views to distinct regions in a
  175: shared spatial positional space, enabling seamless support for an arbitrary number of camera views without
  176: modifying the backbone architecture.
  177: 2.2
  178: Pre-training
  179: Our pre-training data follows a four-level data pyramid [5], organized from broad visual diversity to
  180: embodiment-specific control signals: Internet videos, ego-centric videos, heterogeneous-embodiment data,
  181: and specific-embodiment data. The main motivation is to maximize data efficiency. The bottom level uses
  182: large-scale Internet videos to train the video generation model Vidu [3], which serves as the foundation model
  183: of MotuBrain. The second level introduces ego-centric videos, which provide first-person interaction patterns
  184: and hand-object dynamics that are closer to embodied manipulation. The third level uses heterogeneous-
  185: embodiment data collected from different robot platforms, tasks, and scenes. In our setting, we only use
  186: dual-arm robot data at this level. The top level consists of specific-embodiment data collected on the target
  187: robot configuration, which further aligns the model with the final action space, kinematics, camera setup, and
  188: deployment distribution. This hierarchy reflects a central design principle of MotuBrain: the model should
  189: learn from heterogeneous multimodal data wherever available, rather than restricting supervision to a single
  190: narrow data format.
  191: Starting from the pretrained Vidu weights [3], we perform two-stage pre-training corresponding to the
  192: second and third levels of the data pyramid. In the first stage, we train only the video branch on ego-centric
  193: and heterogeneous-embodiment data, while keeping the randomly initialized action branch unchanged.
```

## Extract 5: training focuses on adapting the model to the target embodiment. We start from the checkpoint obtained; training with a block-causal attention mask. The input sequence contains language tokens, conditioned-image

Lines 315-364

```text
  315: training focuses on adapting the model to the target embodiment. We start from the checkpoint obtained
  316: after the second stage of pre-training and finetune it on specific-embodiment data. In this stage, we consider
  317: two post-training settings, namely a non-autoregressive (Non-AR) setting and an autoregressive (AR) setting,
  318: 5
  319: 
  320: and train separately under each setting. The attention masks used in stage 1, stage 2, Non-AR, and AR are
  321: summarized in Figure 2. The two post-training settings mainly differ in sequence factorization and attention
  322: masking.
  323: Under the Non-AR setting, the model denoises all video and action tokens within the full observation window
  324: in a single forward pass, where ot denotes the observation at step t, zt = E(ot) is its VAE latent, the VAE
  325: temporally compresses every τ consecutive video frames into a single latent frame, and the action stream
  326: operates at fva actions per raw video frame. Each latent frame therefore corresponds to Sa = fvaτ action
  327: tokens. Consequently, predicting K future video latents amounts to generating KSa actions, enabling high-
  328: frequency control while keeping the visual stream compact. As noted in Section 2.2, we continue to apply
  329: the same LingBot-VA-style noisy-conditioning augmentation to the conditioned-frame latent in Non-AR post-
  330: training. This improves robustness to noisy conditioning and helps the model recover from partially corrupted
  331: observations.
  332: For long horizon tasks, we adopt an AR formulation based on chunk-level factorization, as illustrated in
  333: Figure 2(d). We partition each episode into non-overlapping chunks and process all chunks in parallel during
  334: training with a block-causal attention mask. The input sequence contains language tokens, conditioned-image
  335: tokens, clean video tokens from teacher-forced past observations, and the noisy video and noisy action tokens
  336: of the current target chunk. Unlike Non-AR post-training, the AR setting does not apply the noisy-conditioning
  337: augmentation to the conditioned frame. For chunk k, tokens can attend only to clean visual context from
  338: preceding chunks, but never to future chunks. Importantly, our AR model does not involve clean action tokens,
  339: because doing so would break the unified relative-EEF action representation. At deployment, the model rolls
  340: out sequentially, using the newly observed frame as clean context for the next chunk.
  341: To better align post-training with deployment, we adopt V2A-style attention in both operating modes. Under
  342: this attention pattern, action tokens attend to video and language tokens, while video tokens never attend
  343: to action tokens. Combined with the UniDiffuser formulation, where video and action are sampled with
  344: independent timesteps, this asymmetric dependency makes it possible to use an action-only suffix during
  345: inference: after a short joint denoising prefix, the video stream can be frozen and only the action stream
  346: continues to update while attending to the cached visual-language context. This design is motivated by
  347: efficiency rather than a change in training objective, and in practice it enables substantial inference acceleration
  348: without degrading task success rate.
  349: 2.4
  350: Inference
  351: 2.4.1
  352: Inference Optimization
  353: A practical concern with world-action models is inference latency: jointly denoising a high-dimensional video
  354: latent and an action latent over many diffusion steps couples two of the slowest computations in modern
  355: generative modeling, and naively deployed WAMs typically run far below the control rates required for high
  356: frequency manipulation. We adopt and extend a stack of optimizations to tackle this problem. Crucially, we
  357: verify on the RoboTwin2.0 that this speedup is essentially lossless: average task success rates fluctuate within
  358: sub-percent margins across the optimized and unoptimized configurations, indicating that the gains come
  359: from removing genuinely redundant computation rather than from sacrificing model fidelity. We detail each
  360: component below.
  361: Noise sampling.
  362: We use separate SNR-based timestep sampling for the two modalities during training.
  363: Specifically, we set timeshift = 6 for video and timeshift = 1 for action. This makes video timesteps more
  364: likely to be sampled from noisier regions, while action timesteps are sampled more uniformly. As a result, the
```

## Extract 6: keyword window

Lines 348-413

```text
  348: without degrading task success rate.
  349: 2.4
  350: Inference
  351: 2.4.1
  352: Inference Optimization
  353: A practical concern with world-action models is inference latency: jointly denoising a high-dimensional video
  354: latent and an action latent over many diffusion steps couples two of the slowest computations in modern
  355: generative modeling, and naively deployed WAMs typically run far below the control rates required for high
  356: frequency manipulation. We adopt and extend a stack of optimizations to tackle this problem. Crucially, we
  357: verify on the RoboTwin2.0 that this speedup is essentially lossless: average task success rates fluctuate within
  358: sub-percent margins across the optimized and unoptimized configurations, indicating that the gains come
  359: from removing genuinely redundant computation rather than from sacrificing model fidelity. We detail each
  360: component below.
  361: Noise sampling.
  362: We use separate SNR-based timestep sampling for the two modalities during training.
  363: Specifically, we set timeshift = 6 for video and timeshift = 1 for action. This makes video timesteps more
  364: likely to be sampled from noisier regions, while action timesteps are sampled more uniformly. As a result, the
  365: model becomes more robust at predicting accurate actions under noisy visual conditions. This improves both
  366: robustness and convergence, allowing the number of inference steps to be reduced from 50 to 30 without
  367: performance degradation.
  368: Compiling.
  369: We optimize the inference graph and fuse the operators with torch.compile to reduce the
  370: overhead of repeated denoising. Since the model is rewritten as a single-GPU, pure-PyTorch inference model,
  371: 6
  372: 
  373: the main Transformer computation can be compiled directly at inference time. This optimization primarily
  374: improves execution efficiency for the repeated DiT forwards used during sampling.
  375: DiT Cache.
  376: We adopt a DreamZero-style DiT caching strategy [36] to exploit temporal redundancy across
  377: denoising steps. Let vt denote the velocity prediction at denoising step t. We measure the similarity between
  378: two consecutive predictions by
  379: st =
  380: ⟨vt, vt−1⟩
  381: ∥vt∥2 ∥vt−1∥2
  382: .
  383: (7)
  384: When st > γ, where γ is a predefined threshold, we skip a small number of subsequent DiT evaluations and
  385: approximate the skipped predictions from recent history, i.e.,
  386: ˆvt+j ≈vt,
  387: j = 1, . . . , k,
  388: (8)
  389: where k is the cache length. The cache can be applied to either the video velocity or, in action-only mode, the
  390: action velocity, and is reset for each inference call or chunk.
  391: FP8 Quantization.
  392: We further reduce DiT inference cost with FP8 quantization. The implementation replaces
  393: eligible nn.Linear layers with FP8 linear layers. We store the weights in float8_e4m3fn with a per-tensor
  394: scale, dynamically quantize activations to FP8 at runtime, and compute matrix multiplications through
  395: torch._scaled_mm, returning outputs in the original compute dtype. Layers whose input or output dimensions
  396: are not divisible by 16 are skipped to satisfy the kernel alignment requirement. This optimization mainly
  397: targets the large linear projections in attention and MLP blocks, reducing memory bandwidth and GEMM
  398: cost on FP8-capable GPUs. Since quantization is applied after checkpoint loading and before compilation, the
  399: compiled graph traces the quantized linear operators directly.
  400: V2A-Style Inference.
  401: For models trained with V2A attention, the video stream is prevented from attending
  402: to action tokens, while the action stream can still attend to visual and language context. This asymmetric
  403: dependency enables an efficient inference schedule. Instead of denoising video and action jointly for all
  404: sampling steps, we use a short joint denoising prefix followed by an action-only suffix:
  405: (z(t+1)
  406: v
  407: , z(t+1)
  408: a
  409: ) =
  410: 
  411: 
  412: 
  413: Φjoint
```

## Extract 7: keyword window

Lines 470-509

```text
  470: + Noise sampling
  471: 30
  472: 95.0
  473: 2.90
  474: 0.34
  475: 1.69×
  476: + torch.compile
  477: 30
  478: 32.7
  479: 0.98
  480: 1.02
  481: 5.00×
  482: + FP8 quantization
  483: 30
  484: 29.3
  485: 0.88
  486: 1.14
  487: 5.57×
  488: + DiT cache
  489: 30
  490: –
  491: 0.20
  492: 5.00
  493: 24.5×
  494: + V2A-style
  495: 30(action-only)
  496: –
  497: 0.09
  498: 11.11
  499: 54.4×
  500: 2.4.2
  501: Real-time Inference and Execution
  502: Real-time control is essential for robotic deployment, while the inference latency of world action models
  503: is usually non-negligible. MotuBrain therefore decouples the model inference loop from the robot action
  504: execution loop: the controller executes the current action chunk at the target control frequency, while the
  505: world action model asynchronously generates the next chunk from the latest observation.
  506: However, directly switching to the newly generated chunk may introduce chunk-boundary discontinuities,
  507: such as action regression, velocity jumps, and high-frequency jitter, since adjacent chunks may be generated
  508: from different observations and action modes. To reduce such boundary mismatch, we adopt an RTC-inspired
  509: strategy [7]. The unexecuted part of the current chunk is used as a constraint for the next generation and
```

## Extract 8: keyword window

Lines 568-607

```text
  568: To improve robustness under variable inference and communication latency, MotuBrain maintains a delay
  569: queue Q that stores recent inference delays, and the system uses ˆdt+1 = max(Q) as a conservative estimate for
  570: the next inference request. This estimated delay determines the length of the frozen prefix. The fusion window
  571: is also adjusted accordingly: when the estimated delay increases, more steps are treated as fully constrained;
  572: when the estimated delay decreases, more future actions are left to be updated by the new prediction. This
  573: makes the asynchronous execution more stable under fluctuating network and model latency.
  574: 3
  575: Evaluations
  576: In this section, we evaluate MotuBrain in both simulation and real world environment.
  577: 3.1
  578: Evaluation on Simulation Environment
  579: Following the protocol of RoboTwin 2.0 [11], we adopt a multi-task training setup where all models are trained
  580: with 2,500 demonstrations collected from clean scenes (50 per task) together with 25,000 demonstrations
  581: from heavily randomized scenes (500 per task). For training efficiency and temporal consistency across tasks,
  582: we downsample videos to 5 Hz and action sequences to 10 Hz. We further conduct ablation studies over
  583: multiple policy architectures to analyze how different design choices affect the performance of MotuBrain
  584: under the same data and evaluation setting.
  585: Table 3 Robotwin 2.0 Results. Following previous works, MotuBrain is fine-tuned from pre-trained weights on the official
  586: RoboTwin 2.0 dataset (clean + randomized), yielding the evaluation results presented in the table. MotuBrain-Non-AR
  587: represents non-autoregressive mode.
  588: Model
  589: Clean
  590: Randomized
  591: # VLA Based
  592: π0
  593: 65.9
  594: 58.4
  595: X-VLA
  596: 72.9
  597: 72.8
  598: π0.5
  599: 82.7
  600: 76.8
  601: starVLA
  602: 88.2
  603: 88.3
  604: ABot-M0
  605: 81.2
  606: 80.4
  607: LingBot-VLA
```

## Extract 9: keyword window

Lines 1353-1392

```text
 1353: transitions, thereby improving knowledge reuse and cross-task generalization more efficiently than data
 1354: duplication alone.
 1355: Figure 3 Task scaling results. For each point on the curve,
 1356: we train the model using data from only the specified num-
 1357: ber of tasks and keep optimization running until conver-
 1358: gence, i.e., until the average success rate becomes approxi-
 1359: mately stable. As the number of training tasks increases,
 1360: MotuBrain shows a clear upward trend in average success
 1361: rate and consistently stronger scaling behavior than con-
 1362: ventional VLA baselines.
 1363: Figure 4
 1364: Data scaling results.
 1365: For each data budget,
 1366: we uniformly subsample demonstrations from every task
 1367: to keep the per-task data distribution balanced.
 1368: The
 1369: number of training steps is set proportional to the total
 1370: amount of training data; in particular, training on the full
 1371: 27,500-trajectory dataset uses 50,000 optimization steps.
 1372: MotuBrain continues to benefit from additional training
 1373: data, indicating that larger-scale supervision improves pol-
 1374: icy performance and robustness.
 1375: 3.2
 1376: Evaluation on World Models
 1377: Beyond action execution, we also evaluate whether MotuBrain can understand and predict changes in the
 1378: world itself. We adopt WorldArena [31], a unified benchmark that scores embodied world models through 16
 1379: 11
 1380: 
 1381: numerical metrics across six perceptual sub-dimensions—visual quality, motion quality, content consistency,
 1382: physics adherence, 3D accuracy, and controllability. We use the entire 2,500-trajectory release for high-
 1383: resolution training and report benchmark submission results. Because MotuBrain is a unified world-action
 1384: model, the same backbone simultaneously serves as the manipulation policy used in Section 3.1 and as
 1385: the world predictor evaluated here. For WorldArena, we run the model in its forward-dynamics mode
 1386: (FDM), which serves as an action-conditioned world model. We train and run inference at high resolution
 1387: with a 5 Hz video stream paired with 10 Hz actions (1:2 ratio), using classifier-free guidance to improve
 1388: instruction-following capability.
 1389: As shown in Figure 5, MotuBrain attains the highest EWMScore (63.77) among embodied world models and
 1390: video baselines on the WorldArena leaderboard. Its margin over the strongest video-generation baseline shown
 1391: here (Wan2.6, 59.80) is approximately four points. Notably, MotuBrain ranks first on motion quality and
 1392: remains competitive on several other dimensions, indicating that strong dynamics modeling can be achieved
```

## Extract 10: keyword window

Lines 1491-1530

```text
 1491: 0.2811
 1492: Physics
 1493: Adherence
 1494: Interaction Quality
 1495: 0.7174
 1496: 0.8150
 1497: 0.7316
 1498: 0.6262
 1499: 0.8196
 1500: 0.7510
 1501: Trajectory Accuracy
 1502: 0.4793
 1503: 0.1136
 1504: 0.1218
 1505: 0.4820
 1506: 0.3150
 1507: 0.5427
 1508: 3D Accuracy
 1509: Depth Accuracy
 1510: 0.8992
 1511: 0.7427
 1512: 0.7543
 1513: 0.9325
 1514: 0.7199
 1515: 0.9844
 1516: Perspectivity
 1517: 0.9290
 1518: 0.9964
 1519: 0.9394
 1520: 0.8366
 1521: 0.9894
 1522: 0.9560
 1523: Controllability
 1524: Instruction Following
 1525: 0.8072
 1526: 0.9714
 1527: 0.8996
 1528: 0.6768
 1529: 0.9210
 1530: 0.8214
```

